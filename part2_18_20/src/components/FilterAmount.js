const FilterAmount = ({ name, filter, i }) => {
    console.log('Filter to check against', filter)
    console.log('name to check:', name)
    if (name.includes(filter) || filter === '') {
    }
}

export default FilterAmount